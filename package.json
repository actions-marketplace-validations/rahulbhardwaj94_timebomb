{
  "name": "timebomb-workspace",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/core",
    "packages/cli",
    "packages/action"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces --if-present",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
