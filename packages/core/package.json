{
  "name": "timebomb-core",
  "version": "1.0.0",
  "description": "TimeBomb rule engine and AST traversal",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "node --test dist/tests/**/*.js",
    "test:ts": "npx tsx --test tests/**/*.test.ts",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "ts-morph": "^22.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "tsx": "^4.7.0"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "license": "MIT"
}
